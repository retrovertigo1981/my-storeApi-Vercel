name: Deploy to Vercel

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Node.js and npm
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm install

      - name: Deploy to Vercel
        run: npx vercel --token ${{secrets.VERCEL_TOKEN}} --prod --name "mystore-api-postgres" --confirm --env ${{ secrets.POSTGRES_URL }}
